#include <iostream>
#include <cmath>


/*

https://cplusplus.com/reference/cmath/


 */

int main()
{
    double r = 3.14;
    double x = 3;
    double y = 4;
    double z;

    /* z = std::max(x, y); 4 */
    /* z = std::min(x, y); 3 */
    /* z = pow(2,3); */
    /* z = sqrt(9); */
    /* z = abs(-3); // how far is it from zero */
    /* z = round(r);*/
    /* z = ceil(r)  */
    /* z = floor(r); */

    std::cout << z;

    return 0;
}